/**
 * BusinessInfo — content moved into ProfileHeader (description is now
 * rendered inline inside the identity card per the Stitch design).
 * This file is kept to avoid any import-chain breakage and can be
 * restored or extended in the future.
 */
export default function BusinessInfo() {
  return null;
}